{% if content %}
  <nav {{ attributes.addClass('navbar navbar-default navbar-fixed-top main-navigation-itchefer ') }} role="banner">
    <div class="container container--navbar">
      {{ content }}
    </div>
  </nav>
{% endif %}
